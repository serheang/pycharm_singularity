name: build-singularity-container
on:
  # Do something on push
  push:
    # Ediot the branches here
    branches:
      - main
      - master
jobs:
  test-build:
    name: "Test build recipe"
    runs-on: ubuntu-latest
    strategy:
      # Keep going on other deployments if anything bloops
      fail-fast: false
      matrix:
        singularity_version:
          - '2.6'
          - '3.8.4'
          - 'latest'
    container:
      image: quay.io/singularity/singularity:v${{ matrix.singularity_version }}
      options: --privileged
     
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: build pycharm
        run: sudo singularity build pycharm-${{ matrix.singularity_version }}.sif Singularity
      - name: Login and Deploy Container
        run: |         
            echo ${{ secrets.GITHUB_TOKEN }} | singularity remote login -u ${{ secrets.GHCR_USERNAME }} --password-stdin oras://ghcr.io
            singularity push pycharm-${{ matrix.singularity_version }}.sif oras://ghcr.io/${GITHUB_REPOSITORY}:${{ matrix.singularity_version }}
